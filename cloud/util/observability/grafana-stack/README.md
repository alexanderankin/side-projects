# Open Telemetry (full) with Grafana stack components

This folder shows how to implement a development grade
observability service (handle logs, metrics, traces)
with Grafana components:

* loki (logs)
* prometheus (metrics)
* tempo (traces)
